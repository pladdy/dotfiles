#!/usr/bin/env perl

use strict;
use warnings;

##
 # this is just a test, only a test, of vim colors.  yay colors!!!
 ##

my $integer = 1;
my $string = "test-niner";

my @array = [1, 2, "test"];

sub test_sub {
  return 1;
}
